# FORMOCRACY ESC 暂停菜单设计

全局 `PauseMenu` 使用 `CanvasLayer`，层级低于开发者控制台、高于游戏场景。主工作台、日报和验收预览中按 ESC 会暂停场景树并显示菜单；开场与主菜单保留原有 ESC 行为。开发者控制台打开时，ESC 优先关闭控制台。

菜单提供继续游戏、全屏开关、返回主菜单和退出游戏。返回主菜单及桌面退出使用二次确认；Web 环境不调用退出接口。场景切换时必须解除暂停并关闭菜单，透明根层在关闭时不得拦截鼠标。

测试覆盖场景白名单、打开/关闭状态、`SceneTree.paused`、全屏标签以及现有输入回归。
